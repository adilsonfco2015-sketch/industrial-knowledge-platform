export default function LessonCard({ lesson, onEdit, onDelete, onOpen }) {
  const imageStyle = lesson.image ? { backgroundImage: `url("${lesson.image}")` } : undefined;
  return <article className="overflow-hidden rounded-3xl bg-white shadow-sm"><div className="grid grid-cols-1 lg:grid-cols-3">{lesson.image && <div role="img" aria-label={lesson.title} style={imageStyle} className="min-h-[260px] w-full bg-cover bg-center" />}<div className={`${lesson.image ? 'lg:col-span-2' : 'lg:col-span-3'} space-y-5 p-6`}><div className="flex flex-wrap items-center justify-between gap-4"><button onClick={() => onOpen(lesson)} className="text-left"><p className="text-sm text-slate-400">{lesson.id}{lesson.date ? ` • ${lesson.date}` : ''}</p><h2 className="mt-1 text-2xl font-bold text-slate-800 transition hover:text-cyan-700">{lesson.title}</h2></button><span className={`rounded-full px-4 py-2 text-sm font-medium ${lesson.status === 'Resolvido' ? 'bg-green-100 text-green-700' : 'bg-yellow-100 text-yellow-700'}`}>{lesson.status}</span></div><p className="text-slate-600">{lesson.description || 'Sem descrição registrada.'}</p><div className="grid gap-4 md:grid-cols-2"><Info label="Setor" value={lesson.sector} /><Info label="Responsável" value={lesson.responsible || 'Não informado'} /></div><Info label="Causa raiz" value={lesson.cause || 'Não informada'} /><Info label="Ação corretiva" value={lesson.action || 'Não informada'} /><div className="flex gap-3 pt-2"><button onClick={() => onOpen(lesson)} className="rounded-2xl bg-slate-950 px-5 py-3 font-medium text-white">Abrir dossiê</button><button onClick={() => onEdit(lesson)} className="rounded-2xl bg-slate-200 px-5 py-3 font-medium text-slate-700">Editar</button><button onClick={() => onDelete(lesson.id)} className="rounded-2xl bg-red-50 px-5 py-3 font-medium text-red-700">Excluir</button></div></div></div></article>;
}
function Info({ label, value }) { return <div className="rounded-2xl bg-slate-50 p-4"><p className="text-sm text-slate-400">{label}</p><p className="mt-1 font-semibold text-slate-700">{value}</p></div>; }
