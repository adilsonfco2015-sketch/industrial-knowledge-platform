export default function LessonHistory({ lesson, history = [] }) { const fallbackDate = lesson.conclusionDate || lesson.date || 'Data não registrada'; const entries = history.length ? history : [{ id: 'prepared', user: 'Administrador', timestamp: fallbackDate, action: 'Registro disponível', field: 'Histórico', previousValue: '—', newValue: lesson.status }]; return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-xs font-bold tracking-[0.15em] text-cyan-700">HISTÓRICO LOCAL · PREPARADO PARA INTEGRAÇÃO</p><h2 className="mt-2 text-xl font-bold text-slate-900">Histórico da ocorrência</h2><div className="mt-6 space-y-6 border-l-2 border-cyan-200 pl-5">{entries.map((entry) => <article key={entry.id} className="relative"><span className="absolute -left-[1.9rem] top-1 grid h-5 w-5 place-items-center rounded-full bg-cyan-600 text-[10px] text-white">●</span><p className="text-sm font-semibold text-slate-700">{formatDate(entry.timestamp)}</p><p className="mt-1 text-sm text-slate-500">{entry.user}</p><div className="mt-3 rounded-xl bg-slate-50 p-4 text-sm text-slate-600"><p><strong>{entry.action}</strong> · Campo: <strong>{entry.field}</strong></p><p className="mt-2">De <strong>{entry.previousValue || '—'}</strong> para <strong>{entry.newValue || '—'}</strong></p></div></article>)}</div></section>; }
function formatDate(value) { const date = new Date(value); return Number.isNaN(date.getTime()) ? value : date.toLocaleString('pt-BR'); }
