const cards = (lessons, resolvedCount, analysisCount) => [['Total de registros', lessons.length, '▤', 'text-blue-700 bg-blue-50'], ['Problemas resolvidos', resolvedCount, '✓', 'text-emerald-700 bg-emerald-50'], ['Em análise', analysisCount, '◔', 'text-amber-700 bg-amber-50'], ['Setor crítico', 'Pintura', '!', 'text-rose-700 bg-rose-50']];
export default function KPISection({ lessons, resolvedCount, analysisCount }) { return <section className="grid grid-cols-1 gap-4 sm:grid-cols-2 2xl:grid-cols-4">{cards(lessons, resolvedCount, analysisCount).map(([title, value, icon, color]) => <div key={title} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><p className="text-sm font-medium text-slate-500">{title}</p><span className={`grid h-9 w-9 place-items-center rounded-xl font-bold ${color}`}>{icon}</span></div><p className="mt-5 text-3xl font-bold tracking-tight text-slate-900">{value}</p><p className="mt-2 text-xs text-slate-400">Atualizado em tempo real</p></div>)}</section>; }
