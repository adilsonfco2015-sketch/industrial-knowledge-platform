export default function Breadcrumbs({ items }) { return <nav aria-label="Navegação estrutural" className="mb-3 flex flex-wrap items-center gap-2 text-xs font-medium text-slate-400">{items.map((item, index) => <span key={item} className="flex items-center gap-2"><span className={index === items.length - 1 ? 'text-slate-600' : ''}>{item}</span>{index < items.length - 1 && <span>›</span>}</span>)}</nav>; }
